import React from 'react'

const Modal = () => {
    // wrap other componenets???

    // Needs state = open set to false This should be passed down from parent
    //  Once changed to true render component
    // can be changed back to false from inside the rendered component component


    return (
        <div>
            Yas Modal Yasssss
        </div>
    )
}
export default Modal